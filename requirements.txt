# Core Flask application
flask==2.3.3
gunicorn==20.1.0

# PyTorch CPU-only versions (using available versions)
torch==2.5.1+cpu
torchvision==0.20.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Image processing
pillow==10.0.1

# Data science and ML
numpy==1.24.3
scikit-learn==1.3.0

# Remove heavy dependencies for production
# matplotlib==3.10.5  # Not needed for inference
# albumentations==1.4.25  # Not needed for inference
# tensorflow==2.20.0  # Using PyTorch instead
# opencv-python==4.12.0.88  # Not needed for basic inference